import { prisma } from "@/lib/prisma";
import { getCurrentUser } from "@/lib/auth";
import { createProspectLead } from "./actions";
export const dynamic="force-dynamic";
export default async function Prospecting(){const u=await getCurrentUser();if(!u)return null;const leads=await prisma.lead.findMany({where:{workspaceId:u.workspaceId,source:"Prospecting"},orderBy:{score:"desc"},take:50});return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Prospecting</h1><p className="mt-0.5 text-sm text-muted">Build an ICP, capture prospects and qualify them into your sales pipeline.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Add prospect</h2><p className="mt-1 text-sm text-muted">This local MVP captures prospects. Provider/API-based discovery can be connected later from Integrations.</p><form action={createProspectLead} className="mt-4 grid gap-3 md:grid-cols-3"><input required name="email" type="email" placeholder="Email" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="firstName" placeholder="First name" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="lastName" placeholder="Last name" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="title" placeholder="Decision maker / title" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="niche" placeholder="Niche / industry" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="location" placeholder="Location" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="score" type="number" min="0" max="100" placeholder="Lead score" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Add to Leads</button></form></section><section className="rounded-[14px] border border-border bg-surface shadow-[var(--shadow-card)] overflow-hidden"><div className="p-5 border-b border-border"><h2 className="font-semibold">Prospects captured</h2></div><table className="w-full text-sm text-left"><thead className="border-b border-border text-xs uppercase tracking-wide text-muted"><tr><th className="p-4">Prospect</th><th className="p-4">Title</th><th className="p-4">Niche</th><th className="p-4">Location</th><th className="p-4">Score</th></tr></thead><tbody>{leads.map(l=><tr key={l.id} className="border-b border-border last:border-0"><td className="p-4"><div className="font-medium">{[l.firstName,l.lastName].filter(Boolean).join(" ")||l.email}</div><div className="text-xs text-muted">{l.email}</div></td><td className="p-4">{l.title||"—"}</td><td className="p-4">{l.industry||"—"}</td><td className="p-4">{l.location||"—"}</td><td className="p-4 font-semibold">{l.score}</td></tr>)}</tbody></table></section></div></>}
