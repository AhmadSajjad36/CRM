import { prisma } from "@/lib/prisma";
import { getCurrentUser } from "@/lib/auth";
import { updateWorkspace } from "./actions";
export const dynamic="force-dynamic";
export default async function Settings(){const u=await getCurrentUser();if(!u)return null;const workspace=await prisma.workspace.findUnique({where:{id:u.workspaceId},include:{_count:{select:{users:true,companies:true,contacts:true,leads:true,opportunities:true}}}});if(!workspace)return null;return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Settings</h1><p className="mt-0.5 text-sm text-muted">Workspace preferences, ownership and CRM configuration.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="max-w-2xl rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Workspace</h2><form action={updateWorkspace} className="mt-4 space-y-3"><label className="block text-sm font-medium">Workspace name<input required name="name" defaultValue={workspace.name} className="mt-1 w-full rounded-lg border border-border px-3 py-2.5"/></label><label className="block text-sm font-medium">Workspace slug<input required name="slug" defaultValue={workspace.slug} className="mt-1 w-full rounded-lg border border-border px-3 py-2.5"/></label><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Save workspace</button></form></section><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{Object.entries(workspace._count).map(([k,v])=><div key={k} className="rounded-xl border border-border bg-surface p-4"><p className="text-xs uppercase tracking-wide text-muted">{k}</p><p className="mt-2 font-mono text-xl font-semibold">{v}</p></div>)}</section></div></>}
