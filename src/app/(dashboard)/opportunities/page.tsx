import { prisma } from "@/lib/prisma"; import { getCurrentUser } from "@/lib/auth"; import { createOpportunity, deleteOpportunity } from "./actions";
export const dynamic="force-dynamic";
export default async function OpportunitiesPage(){const u=await getCurrentUser();if(!u)return null;const [rows,pipeline]=await Promise.all([prisma.opportunity.findMany({where:{workspaceId:u.workspaceId},orderBy:{createdAt:"desc"},include:{stage:true,company:true,contact:true}}),prisma.pipeline.findFirst({where:{workspaceId:u.workspaceId,isDefault:true},include:{stages:{orderBy:{position:"asc"}}}})]);return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Opportunities</h1><p className="mt-0.5 text-sm text-muted">Qualified revenue tracked through your sales pipeline.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Add opportunity</h2><form action={createOpportunity} className="mt-4 grid gap-3 md:grid-cols-4"><input name="name" required placeholder="Opportunity name" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="amount" type="number" min="0" placeholder="Amount" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="probability" type="number" min="0" max="100" placeholder="Probability %" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><select name="stageId" className="rounded-lg border border-border px-3 py-2.5 text-sm">{pipeline?.stages.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Add Opportunity</button></form></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.map(o=><article key={o.id} className="rounded-[14px] border border-border bg-surface p-5 shadow-[var(--shadow-card)]"><div className="flex justify-between gap-4"><div><h3 className="font-semibold">{o.name}</h3><p className="mt-1 text-xs text-muted">{o.stage.name}</p></div><form action={deleteOpportunity}><input type="hidden" name="id" value={o.id}/><button className="text-xs text-red-600">Delete</button></form></div><p className="mt-5 font-mono text-xl font-semibold">${o.amount.toLocaleString()}</p><p className="mt-1 text-xs text-muted">{o.probability}% probability</p></article>)}</section></div></>}
