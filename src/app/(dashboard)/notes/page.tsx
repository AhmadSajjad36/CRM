import{prisma}from"@/lib/prisma";import{getCurrentUser}from"@/lib/auth";import{createNote}from"./actions";export const dynamic="force-dynamic";
export default async function Notes(){const u=await getCurrentUser();if(!u)return null;const rows=await prisma.note.findMany({where:{workspaceId:u.workspaceId},orderBy:{createdAt:"desc"},take:50});return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Notes</h1><p className="mt-0.5 text-sm text-muted">Workspace notes that can later be attached to contacts, leads and opportunities.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">New note</h2><form action={createNote} className="mt-4 flex gap-3"><textarea required name="content" placeholder="Write a note..." className="min-h-24 flex-1 rounded-lg border border-border bg-white px-3 py-2.5 text-sm"/><button className="self-end rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Save Note</button></form></section><section className="grid gap-3 md:grid-cols-2">{rows.map(n=><article key={n.id} className="rounded-xl border border-border bg-surface p-4"><p className="whitespace-pre-wrap text-sm leading-6">{n.content}</p><p className="mt-3 text-xs text-muted">{n.createdAt.toLocaleString()}</p></article>)}</section></div></>}
