import { prisma } from "@/lib/prisma";
import { getCurrentUser } from "@/lib/auth";
import { createTag, deleteTag } from "./actions";
export const dynamic="force-dynamic";
export default async function TagsPage(){const u=await getCurrentUser();if(!u)return null;const tags=await prisma.tag.findMany({where:{workspaceId:u.workspaceId},orderBy:{name:"asc"}});return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Tags</h1><p className="mt-0.5 text-sm text-muted">Workspace-level labels for organizing CRM records.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Create tag</h2><form action={createTag} className="mt-4 flex max-w-xl gap-3"><input required name="name" placeholder="e.g. High Value" className="flex-1 rounded-lg border border-border bg-white px-3 py-2.5 text-sm"/><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Add Tag</button></form></section><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{tags.map(t=><div key={t.id} className="rounded-xl border border-border bg-surface p-4 flex items-center justify-between"><div><p className="font-medium">{t.name}</p><p className="text-xs text-muted">{t.slug}</p></div><form action={deleteTag}><input type="hidden" name="id" value={t.id}/><button className="text-xs font-semibold text-red-600">Delete</button></form></div>)}</section></div></>}
