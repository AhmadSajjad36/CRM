import{prisma}from"@/lib/prisma";import{getCurrentUser}from"@/lib/auth";import{createActivity}from"./actions";export const dynamic="force-dynamic";
export default async function Activities(){const u=await getCurrentUser();if(!u)return null;const rows=await prisma.activity.findMany({where:{workspaceId:u.workspaceId},orderBy:{occurredAt:"desc"},take:50});return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Activities</h1><p className="mt-0.5 text-sm text-muted">Calls, emails, meetings, notes and follow-ups.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Log activity</h2><form action={createActivity} className="mt-4 grid gap-3 md:grid-cols-4"><select name="type" className="rounded-lg border border-border px-3 py-2.5 text-sm"><option>CALL</option><option>EMAIL</option><option>MEETING</option><option>NOTE</option><option>FOLLOW_UP</option></select><input required name="subject" placeholder="Subject" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="description" placeholder="Description" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Log Activity</button></form></section><section className="space-y-3">{rows.map(a=><div key={a.id} className="rounded-xl border border-border bg-surface p-4"><div className="flex justify-between"><span className="text-xs font-semibold text-accent">{a.type}</span><span className="text-xs text-muted">{a.occurredAt.toLocaleString()}</span></div><p className="mt-2 font-medium">{a.subject}</p>{a.description&&<p className="mt-1 text-sm text-muted">{a.description}</p>}</div>)}</section></div></>}
