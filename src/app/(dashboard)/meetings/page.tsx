import{prisma}from"@/lib/prisma";import{getCurrentUser}from"@/lib/auth";import{createMeeting}from"./actions";export const dynamic="force-dynamic";
export default async function Meetings(){const u=await getCurrentUser();if(!u)return null;const rows=await prisma.meeting.findMany({where:{workspaceId:u.workspaceId},orderBy:{startsAt:"asc"}});return <><header className="border-b border-border px-6 py-5 md:px-10"><h1 className="font-display text-2xl font-semibold">Meetings</h1><p className="mt-0.5 text-sm text-muted">Schedule and track meetings connected to CRM records.</p></header><div className="space-y-6 px-6 py-8 md:px-10"><section className="rounded-[14px] border border-border bg-surface p-6"><h2 className="font-semibold">Schedule meeting</h2><form action={createMeeting} className="mt-4 grid gap-3 md:grid-cols-4"><input required name="title" placeholder="Meeting title" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input required name="startsAt" type="datetime-local" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="location" placeholder="Location / video link" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><input name="notes" placeholder="Notes" className="rounded-lg border border-border px-3 py-2.5 text-sm"/><button className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-accent-fg">Schedule</button></form></section><section className="space-y-3">{rows.map(m=><div key={m.id} className="rounded-xl border border-border bg-surface p-4"><p className="font-medium">{m.title}</p><p className="mt-1 text-sm text-muted">{m.startsAt.toLocaleString()}{m.location?` · ${m.location}`:""}</p></div>)}</section></div></>}
